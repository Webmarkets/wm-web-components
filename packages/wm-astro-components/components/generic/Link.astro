---
export interface Props {
    title: string;
    href: string;
    newTab?: boolean;
}

const { title, href, newTab = false } = Astro.props as Props;
---

<>
    <a href={href} target={newTab ? "_blank" : "" } rel={newTab ? "noreferrer noopener" : "" }>{title}</a>
</>